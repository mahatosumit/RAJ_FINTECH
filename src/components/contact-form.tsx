'use client';
import {useState} from 'react';
export function ContactForm({lang}:{lang:string}){const [state,setState]=useState<'idle'|'busy'|'ok'|'error'>('idle');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('busy');const form=new FormData(e.currentTarget);const res=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});if(res.ok){setState('ok');e.currentTarget.reset()}else setState('error')}
const ne=lang!=='en';return <form onSubmit={submit} className="rounded-2xl border bg-white p-6 shadow-sm sm:p-8"><h2 className="text-2xl font-bold text-[#103c32]">{ne?'सन्देश पठाउनुहोस्':'Send a message'}</h2><div className="mt-6 grid gap-5 sm:grid-cols-2"><Field name="name" label={ne?'नाम *':'Name *'} required/><Field name="phone" label={ne?'फोन *':'Phone *'} required/><Field name="email" label={ne?'इमेल':'Email'} type="email"/><Field name="subject" label={ne?'विषय *':'Subject *'} required/><div className="hidden"><input name="website_confirm" tabIndex={-1} autoComplete="off"/></div><label className="sm:col-span-2"><span className="form-label">{ne?'सन्देश *':'Message *'}</span><textarea className="form-input min-h-36" name="message" required minLength={10} maxLength={3000}/></label></div><button disabled={state==='busy'} className="btn-primary mt-6" type="submit">{state==='busy'?(ne?'पठाउँदै…':'Sending…'):(ne?'सन्देश पठाउनुहोस्':'Send Message')}</button><div aria-live="polite" className="mt-4 text-sm font-semibold">{state==='ok'&&<p className="text-emerald-700">{ne?'तपाईंको सन्देश प्राप्त भयो। धन्यवाद।':'Your message has been received. Thank you.'}</p>}{state==='error'&&<p className="text-red-700">{ne?'सन्देश पठाउन सकिएन। कृपया पछि प्रयास गर्नुहोस्।':'Message could not be sent. Please try again.'}</p>}</div></form>}
function Field({name,label,type='text',required=false}:{name:string;label:string;type?:string;required?:boolean}){return <label><span className="form-label">{label}</span><input className="form-input" name={name} type={type} required={required} maxLength={150}/></label>}
