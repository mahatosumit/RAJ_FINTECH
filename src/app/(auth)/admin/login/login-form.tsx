'use client';import {useActionState} from 'react';import {login} from './actions';export function LoginForm(){const [error,action,pending]=useActionState(login,undefined);return <form action={action} className="mt-8 space-y-5"><label><span className="form-label">Email address</span><input name="email" type="email" autoComplete="username" required className="form-input"/></label><label><span className="form-label">Password</span><input name="password" type="password" autoComplete="current-password" required minLength={8} className="form-input"/></label>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700" role="alert">{error}</p>}<button className="btn-primary w-full justify-center" disabled={pending}>{pending?'Signing in…':'Sign in securely'}</button></form>}